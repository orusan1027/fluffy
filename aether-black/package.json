{
  "name": "aether-black",
  "version": "3.0.0",
  "description": "AETHER BLACK — End-to-end automated asset factory (Unity / Fab.com)",
  "main": "dashboard/server.js",
  "scripts": {
    "prestart":          "node scripts/check-deps.js",
    "start":             "node dashboard/server.js",
    "setup":             "npm install && npx playwright install chromium && npm --prefix portal-agent install",
    "install:browsers":  "npx playwright install chromium"
  },
  "dependencies": {
    "cors":       "^2.8.5",
    "dotenv":     "^16.4.5",
    "express":    "^4.19.2",
    "playwright": "^1.44.0"
  },
  "engines": { "node": ">=18.0.0" }
}
