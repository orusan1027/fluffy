{
  "name": "portal-agent-crawlers",
  "version": "0.1.0",
  "private": true,
  "description": "DMM/FANZA and DLsite affiliate crawlers for AetherBlack portal",
  "type": "module",
  "scripts": {
    "dmm:crawl": "node --loader ts-node/esm crawlers/dmm/index.ts",
    "dlsite:crawl": "node --loader ts-node/esm crawlers/dlsite/index.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
